<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:orderInCategory="25">
        <item
            android:id="@+id/lyric_list_search"
            android:icon="@drawable/ic_search_black_24dp"
            android:title="Search Gallery"
            app:showAsAction="ifRoom|collapseActionView"
            app:actionLayout="@layout/search_gallery_layout"
            />

        <item
            android:id="@+id/narrow_query"
            android:icon="@drawable/ic_filter_list_black_24dp"
            android:title="Narrow Search"
            app:showAsAction="ifRoom"
            android:visible="false"
            />

        <item
            android:id="@+id/sort_items"
            android:icon="@drawable/ic_sort_black_24dp"
            android:title="Sort Items"
            app:showAsAction="ifRoom"
            />
    </group>
</menu>