<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--<item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <style name="TrackInfo_Subordinate" parent="@android:style/TextAppearance">
        <item name="android:shadowDx">@fraction/shadowDx</item>
        <item name="android:shadowDy">@fraction/shadowDy</item>
        <item name="android:shadowRadius">@fraction/shadowRadius</item>
        <item name="android:shadowColor">@color/trackInfoShadowColor</item>
    </style>

    <style name="TrackInfo" parent="@style/TrackInfo_Subordinate">
        <item name="android:textSize">18sp</item>
    </style>

</resources>
